{
  "name": "fast-with-context",
  "main": "index.js",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "reinstall": "rm -rf node_modules package-lock.json && npm install"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  },
  "dependencies": {
    "@microsoft/fast-element": "^2.9.1",
    "@microsoft/fast-foundation": "^2.50.0"
  }
}
